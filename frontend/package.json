{
  "name": "clear-gdpr-frontend",
  "version": "1.0.0",
  "private": true,
  "license": "LGPL-3.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ClearGDPR/ClearGDPR.git"
  },
  "scripts": {
    "compile": "npm run lerna run compile",
    "prebootstrap": "yarn install",
    "postinstall": "npm run lerna bootstrap",
    "pretest": "npm run compile",
    "test": "npm run testonly",
    "posttest": "yarn run lint",
    "testonly": "npm run lerna run test",
    "coverage": "npm run lerna run coverage",
    "circle": "npm run lerna run coverage && lerna run circle",
    "release": "npm run lerna publish --exact",
    "lerna": "lerna"
  },
  "devDependencies": {
    "lerna": "^3.0.0-rc.0"
  }
}
